# CS4210-ML-AgePredictingModel-Project

This project trains a decision tree model to predict whether a person is in the Adult or Senior age group using NHANES health data.

## Run

```bash
pip install -r requirements.txt
python decision_tree.py
```

The script compares the original depth-4 decision tree with a tuned decision tree that optimizes Senior-class F1 score.
